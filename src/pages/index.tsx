import { FC } from "react";

const HomePage: FC = () => (
  <div
    style={{
      display: "flex",
      flex: 1,
      width: "100%",
      height: "100%",
      justifyContent: "center",
      alignItems: "center",
    }}
  >
        Hello world!
  </div>
);

export default HomePage;
